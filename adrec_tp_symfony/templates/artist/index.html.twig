{% extends 'base.html.twig' %}

{% block title %}Artist index{% endblock %}

{% block body %}
    <div class="section-header-action">
        <h1>Artistes</h1>
        <a class="btn btn-primary" href="{{ path('artist_new') }}"><i class="fas fa-plus-circle"></i> Ajouter un nouvel Artiste</a>
    </div>

    <table class="table table-dark table-striped ">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Nationality</th>
                <th>Beginning_year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artist in artists %}
            <tr>
                <td>{{ artist.id }}</td>
                <td>{{ artist.name }}</td>
                <td>{{ artist.nationality }}</td>
                <td>{{ artist.beginningYear }}</td>
                <td>
                    <a href="{{ path('artist_show', {'id': artist.id}) }}"><i class="fas fa-eye"></i> Détails</a> |
                    <a href="{{ path('artist_edit', {'id': artist.id}) }}"><i class="fas fa-pen"></i> Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    
{% endblock %}
