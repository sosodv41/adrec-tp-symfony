{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Edit Genre{% endblock %}

{% block body %}
    <div class="section-header-action mb-4">
        <h1>Editer Genre</h1>
        <div class="header-buttons-group">
            {{ include('genre/_delete_form.html.twig') }}
            <a class="btn btn-secondary" href="{{ path('genre_index') }}"><i class="fas fa-arrow-circle-left"></i> Retour</a>
        </div>
    </div>

    {{ include('genre/_form.html.twig', {'button_label': 'Valider'}) }}

{% endblock %}
